# デザインQA

final result: passed

## 対象

- ユーザー提供の参照画像と、ローカル実装の編集ダイアログを並べて比較。
- 実際のToDo内容を含む確認画像は、公開リポジトリへ保存しない。

## 確認した状態

- PC: ToDo一覧で「月次資料を確認する」をダブルクリックし、既存値が入った「ToDoを編集」ダイアログが開いた状態。
- スマホ: 390 × 844 px。ダイアログ幅は表示領域内に収まり、横スクロールは発生しないことを確認。
- 比較: 参照画像と実装結果を同じ比較画面に並べ、ヘッダー、タイトル、メモ、日付、期限時刻、リマインダー、繰り返し、状態、タブ、依頼情報の配置を目視確認。

## 操作確認

- ToDoタイトルのダブルクリックで、タイトルだけの直接編集ではなく詳細編集ダイアログが開く。
- ToDoタイトルにフォーカスして Enter キーを押した場合も詳細編集ダイアログが開く。
- 鉛筆ボタンでは従来どおりタイトルだけを直接編集できる。
- 直接編集の Escape キャンセルが動作する。
- 編集ダイアログを閉じたあと、画面上にダイアログが残らない。

## 判定

- P0: なし
- P1: なし
- P2: なし
- ブラウザの warning / error: 0件

今回の変更は編集ダイアログ自体のデザイン変更ではなく、タイトルから同じダイアログを開く操作変更です。参照画像の既存デザインをそのまま利用し、PC・スマホ双方でレイアウト崩れがないことを確認しました。
